{
  "name": "video-downloader",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
  "prestart": "pip install -U yt-dlp --break-system-packages && curl -fsSL https://deno.land/install.sh | sh && mkdir -p /tmp && cp /etc/secrets/cookies.txt /tmp/cookies.txt || true",
  "start": "PATH=$PATH:/opt/render/.local/bin:$HOME/.deno/bin node src/app.js",
  "dev": "nodemon src/app.js"
},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "helmet": "^8.1.0",
    "morgan": "^1.10.1",
    "yt-dlp-wrap": "^2.3.12"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
